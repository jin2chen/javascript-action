name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

env:
  AFTER_BUILD: |
    echo 'a'
    echo 'b'
    echo 'c'

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - run: |
      ${{ env.AFTER_BUILD }}
